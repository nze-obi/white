"use client";
import { LogOut,Search } from "lucide-react"; import { useRouter } from "next/navigation"; import { Button,Input } from "@/components/ui"; import { useCloudContext } from "@/components/cloud-context";
export function Topbar(){ const r=useRouter(); const c=useCloudContext(); async function logout(){await fetch('/api/auth/logout',{method:'POST'});r.replace('/login');r.refresh()} return <header className="sticky top-0 z-20 flex min-h-16 items-center gap-3 border-b border-slate-200 bg-white/95 px-4 backdrop-blur lg:px-6"><div className="relative hidden max-w-sm flex-1 md:block"><Search className="absolute left-3 top-3 h-4 w-4 text-slate-400"/><Input aria-label="Search" placeholder="Search cloud resources…" className="pl-9"/></div><div className="ml-auto flex items-center gap-2"><Input aria-label="Project ID" title="Current project ID" value={c.projectId} onChange={e=>c.setProjectId(e.target.value)} placeholder="Project ID" className="hidden w-52 sm:block"/><Input aria-label="Availability zone" title="Current availability zone" value={c.availabilityZone} onChange={e=>c.setAvailabilityZone(e.target.value)} placeholder="Availability zone" className="hidden w-40 md:block"/><Button variant="ghost" onClick={logout}><LogOut className="h-4 w-4"/><span className="hidden sm:inline">Sign out</span></Button></div></header> }

import { NextResponse } from "next/server"; export async function POST(){ const r=NextResponse.json({ok:true}); r.cookies.set("nobus_token","",{path:"/",maxAge:0}); r.cookies.set("nobus_api_key","",{path:"/",maxAge:0}); return r; }

import { ResourceBrowser } from "@/components/resource-browser"; export default function Page(){return <ResourceBrowser title="Subnets" description="Manage IP ranges, gateways and DHCP settings." path="/api/v3/network/subnet/list" needsProject={true} createOperationId="7b834ebe_controller_create_subnet_api" columns={["id", "name", "network_id", "cidr", "ip_version", "gateway_ip", "enable_dhcp"]}/>}
